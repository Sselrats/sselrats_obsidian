
# 대출청진기 (MoneyLab) - DB 구조 및 워크플로우 제안서

## 1. 전체 아키텍처

### 백엔드 구조
- **단일 통합 백엔드**: FastAPI 기반 단일 백엔드로 3개 프론트엔드 모두 서비스
- **모듈 구조**: 도메인별로 모듈 분리 (auth, customers, consultations, tasks, policies, admin)
- **데이터베이스**: 
  - 프로토타입: In-memory (Python dict)
  - 프로덕션: PostgreSQL (AWS Seoul 또는 Naver Cloud)

### 프론트엔드 구조
- **Frontend 1**: 대출청진기 (고객용) - 우선순위 1
- **Frontend 2**: 고객수첩 (상담사용) - 우선순위 2
- **Frontend 3**: Admin (관리자용) - 우선순위 1

---

## 2. 데이터베이스 스키마

### 2.1 사용자 및 인증

#### users (사용자)
```
- id: int (PK)
- email: string (unique)
- name: string
- phone: string
- role: enum (super_admin, admin, consultant)
- password_hash: string
- mfa_required: boolean
- last_login_at: datetime
- created_at: datetime
- status: enum (active, inactive)
```

#### sessions (세션)
```
- id: int (PK)
- user_id: int (FK → users)
- token: string
- refresh_token: string
- ip: string
- user_agent: string
- created_at: datetime
- expires_at: datetime
```

### 2.2 고객 및 상담

#### customers (고객)
```
- id: int (PK)
- name: string
- phone: string
- birth: string (optional)
- gender: string (optional)
- job: enum (employee, business, freelancer, housewife, unemployed)
- income_band: string (예: "2400-5000만원")
- credit_band_kcb: string (예: "700+")
- credit_band_nice: string (예: "700+")
- region: string
- status: enum (등록, 상담중, 조회중, 심사중, 승인, 실행)
- consultant_id: int (FK → users, nullable)
- created_at: datetime
- updated_at: datetime
```

#### properties (담보 물건)
```
- id: int (PK)
- customer_id: int (FK → customers)
- property_type: enum (apartment, commercial, officetel, villa, house, other)
- address: string
- dong: string (optional)
- ho: string (optional)
- area_m2: float (optional)
- kb_price_low: int
- kb_price_high: int
- kb_price_estimate: int
- owner: string (optional)
- house_count: string (optional)
- tenant: string (optional)
- created_at: datetime
```

#### existing_loans (기존 대출)
```
- id: int (PK)
- customer_id: int (FK → customers)
- bank: string
- amount: int
- start_date: date (optional)
- notes: string (optional)
```

#### consultations (상담)
```
- id: int (PK)
- customer_id: int (FK → customers)
- purpose: enum (purchase, liquidity, debt_consolidation, lease_return)
- desired_amount: int
- channel: string (optional)
- phone_verified: boolean
- status: enum (
    intake_started,
    intake_completed,
    phone_verified,
    offers_generated,
    consultation_requested,
    assigned,
    in_progress,
    approved,
    executed,
    closed
  )
- created_at: datetime
- updated_at: datetime
```

#### consultation_memos (상담 메모)
```
- id: int (PK)
- consultation_id: int (FK → consultations)
- author_id: int (FK → users)
- content: text
- created_at: datetime
```

### 2.3 금융 상품 및 정책

#### institutions (금융기관)
```
- id: int (PK)
- name: string
- category: enum (bank, nonbank, insurance, p2p)
- contact: string (optional)
- active: boolean
```

#### products (금융 상품)
```
- id: int (PK)
- institution_id: int (FK → institutions)
- name: string
- product_type: string
- notes: text (optional)
- active: boolean
```

#### product_policies (상품 정책)
```
- id: int (PK)
- product_id: int (FK → products)
- region_tier: string (급지: A, B, C 등)
- property_type: enum (apartment, commercial, officetel, villa, house, other)
- ltv_max: float (최대 LTV 비율, 예: 0.70)
- dsr_cap: float (DSR 한도, 예: 0.40)
- credit_min_band: string (최소 신용등급, 예: "KCB700+")
- employment_allowed: json (허용 직업 유형)
- income_min: int (최소 소득)
- rate_min: float (최소 금리)
- rate_max: float (최대 금리)
- metadata: json (추가 규칙)
- effective_from: date
- effective_to: date (optional)
- active: boolean
- policy_version: string
```

#### offers (대출 제안)
```
- id: int (PK)
- consultation_id: int (FK → consultations)
- institution_id: int (FK → institutions)
- product_id: int (FK → products)
- max_amount: int
- rate_min: float
- rate_max: float
- ltv_used: float
- dsr_used: float
- notes: text (optional)
- created_at: datetime
- policy_version: string
```

### 2.4 업무 관리

#### tasks (할일)
```
- id: int (PK)
- title: string
- description: text (optional)
- due_date: date
- priority: enum (high, medium, low)
- status: enum (todo, in_progress, done)
- customer_id: int (FK → customers, nullable)
- consultation_id: int (FK → consultations, nullable)
- assignee_id: int (FK → users)
- created_at: datetime
- completed_at: datetime (optional)
```

#### inquiry_requests (조회 요청)
```
- id: int (PK)
- consultation_id: int (FK → consultations)
- requested_by_id: int (FK → users)
- requested_at: datetime
- status: enum (pending, completed)
```

#### inquiry_responses (조회 응답)
```
- id: int (PK)
- inquiry_id: int (FK → inquiry_requests)
- consultant_id: int (FK → users)
- institution_id: int (FK → institutions)
- max_amount: int
- interest_rate_str: string
- responded_at: datetime
```

### 2.5 시스템

#### verification_codes (인증 코드 - Mock)
```
- id: int (PK)
- phone: string
- code: string
- consultation_id: int (FK → consultations)
- expires_at: datetime
- used: boolean
```

#### audit_logs (감사 로그)
```
- id: int (PK)
- actor_id: int (FK → users, nullable)
- action: string
- entity: string
- entity_id: int
- before: json (optional)
- after: json (optional)
- ip: string
- user_agent: string
- created_at: datetime
```

#### system_settings (시스템 설정)
```
- key: string (PK)
- value: json
- updated_at: datetime
```

---

## 3. MVP 워크플로우 (우선순위 1: 대출청진기 + Admin)

### 3.1 고객 상담 신청 플로우

```
1. 고객이 대출청진기 랜딩 페이지 방문
   ↓
2. "대화로 상담 시작" 클릭 → 챗봇 인터페이스 오픈
   ↓
3. 챗봇이 질문 (직업, 소득, 담보 물건, 대출 목적 등)
   ↓
4. 고객이 정보 입력 완료
   → POST /api/consultations (상담 생성, status: intake_completed)
   ↓
5. 휴대폰 본인 인증 요청
   → POST /api/consultations/{id}/verify-phone (Mock: 코드 "000000")
   → status: phone_verified
   ↓
6. 대출 매칭 알고리즘 실행
   → POST /api/consultations/{id}/match
   → product_policies 기반으로 2-3개 금융사 제안 생성
   → offers 테이블에 저장
   → status: offers_generated
   ↓
7. 결과 화면 표시 (금융사, 한도, 금리 범위)
   ↓
8. 고객이 "전문 상담 신청" 클릭
   → POST /api/consultations/{id}/request
   → status: consultation_requested
   ↓
9. 관리자/상담사가 Admin 페이지에서 신규 상담 확인
   → GET /api/admin/consultations?status=consultation_requested
   ↓
10. 관리자가 상담사 배정
    → PATCH /api/consultations/{id} (consultant_id 설정)
    → status: assigned
    → 자동으로 Task 생성 (assignee: 배정된 상담사)
    ↓
11. 상담사가 고객에게 연락 및 상담 진행
    → status: in_progress
    ↓
12. 대출 승인 및 실행
    → status: approved → executed
```

### 3.2 관리자 워크플로우

```
1. Admin 로그인
   → POST /api/auth/login
   ↓
2. 대시보드에서 신규 상담 확인
   → GET /api/admin/consultations?status=consultation_requested
   ↓
3. 고객 상세 정보 확인
   → GET /api/consultations/{id}
   → 고객 정보, 담보 물건, 제안된 상품 확인
   ↓
4. 상담사 배정
   → PATCH /api/consultations/{id}
   ↓
5. 진행 상황 모니터링
   → GET /api/admin/consultations (필터링, 정렬, 페이징)
   ↓
6. 상담 메모 추가
   → POST /api/consultations/{id}/memos
```

---

## 4. API 엔드포인트 (우선순위 1)

### 4.1 인증
- `POST /api/auth/login` - 로그인
- `GET /api/auth/me` - 현재 사용자 정보

### 4.2 상담
- `POST /api/consultations` - 상담 생성 (고객 정보 + 담보 정보)
- `GET /api/consultations/{id}` - 상담 상세 조회
- `POST /api/consultations/{id}/verify-phone` - 휴대폰 인증 (Mock)
- `POST /api/consultations/{id}/match` - 대출 매칭 실행
- `POST /api/consultations/{id}/request` - 전문 상담 신청
- `POST /api/consultations/{id}/memos` - 상담 메모 추가

### 4.3 관리자
- `GET /api/admin/consultations` - 상담 목록 (필터링, 정렬, 페이징)
- `PATCH /api/consultations/{id}` - 상담 정보 수정 (상담사 배정, 상태 변경)
- `GET /api/admin/customers` - 고객 목록
- `GET /api/admin/customers/{id}` - 고객 상세 조회

### 4.4 업무
- `GET /api/tasks` - 할일 목록
- `POST /api/tasks` - 할일 생성
- `PATCH /api/tasks/{id}` - 할일 수정

### 4.5 정책
- `GET /api/policies` - 금융사 정책 조회 (관리자 전용)

---

## 5. 대출 매칭 알고리즘 (간소화 버전)

### 5.1 입력 데이터
- 고객 정보: 직업, 소득 구간, 신용 등급
- 담보 정보: 물건 유형, 지역, 시세
- 대출 니즈: 희망 금액, 용도

### 5.2 매칭 로직
```python
1. LTV 계산
   LTV = 희망 금액 / 담보 시세
   
2. DSR 계산 (간소화)
   월 상환액 = 희망 금액 × 금리 / 12
   DSR = 월 상환액 / (월 소득 / 12)
   
3. product_policies 테이블에서 조건 필터링
   - property_type 일치
   - region_tier 일치
   - LTV <= ltv_max
   - DSR <= dsr_cap
   - 신용 등급 >= credit_min_band
   - 직업 in employment_allowed
   - 소득 >= income_min
   
4. 매칭된 상품 2-3개 반환
   - 금융사명
   - 최대 한도
   - 금리 범위 (rate_min ~ rate_max)
   - 주의사항: "예상 결과이며, 실제 심사 시 변동될 수 있습니다"
```

---

## 6. 프론트엔드 통합 전략

### 6.1 v0 코드 활용
- v0의 UI 컴포넌트 및 디자인 그대로 사용
- `lib/storage.ts` → `lib/api.ts`로 교체
  - 동일한 함수명 유지 (getCustomers, addCustomer 등)
  - localStorage 호출 → API 호출로 변경
  - 데이터 구조는 최대한 유지

### 6.2 변경 최소화
- 컴포넌트 로직은 최소한만 수정
- 로딩 상태, 에러 처리 추가
- 인증 토큰 관리 추가

---

## 7. 보안 및 규제 준수

### 7.1 프로토타입 단계 (현재)
- ✅ 가짜 데이터만 사용 (실제 개인정보 수집 금지)
- ✅ 보안 헤더 설정 (CSP, HSTS, X-Frame-Options)
- ✅ CORS 설정 (프론트엔드 도메인만 허용)
- ✅ 감사 로그 기록 (모든 중요 작업)
- ✅ 신용 점수는 구간으로만 저장 (정확한 점수 저장 금지)
- ✅ 전화번호 마스킹 (로그 및 UI 목록)

### 7.2 프로덕션 단계 (라이센스 신청 전)
- 🔒 한국 리전 클라우드 이전 (AWS Seoul / Naver Cloud)
- 🔒 TLS 1.2+ 전송 암호화
- 🔒 KMS 저장 암호화
- 🔒 관리자 MFA 필수
- 🔒 WAF 및 DDoS 방어
- 🔒 DB 프라이빗 서브넷
- 🔒 중앙 집중식 불변 로그
- 🔒 백업 및 복구 테스트
- 🔒 취약점 점검 및 모의해킹

---

## 8. 개발 일정 (예상)

### Phase 1: 프로토타입 (현재 세션, ~3시간)
- ✅ v0 코드 분석 완료
- ⏳ DB 구조 및 워크플로우 합의
- ⏳ FastAPI 백엔드 개발
  - 인증 API
  - 상담 API
  - 매칭 알고리즘 (간소화)
  - 관리자 API
- ⏳ 프론트엔드 통합 (대출청진기 + Admin)
- ⏳ 로컬 테스트
- ⏳ 배포 (Fly.io + Vercel)
- ⏳ 공개 URL 공유

### Phase 2: 고객수첩 통합 (1-2주)
- 고객수첩 프론트엔드 통합
- 상담사 기능 추가
- 업무 관리 고도화

### Phase 3: 프로덕션 준비 (2-4주)
- PostgreSQL 마이그레이션
- AWS Seoul / Naver Cloud 이전
- 보안 강화
- 실제 API 연동 (휴대폰 인증, 주소 검색 등)
- 금융사 정책 DB 구축
- 취약점 점검

### Phase 4: 라이센스 신청 (4-8주)
- 금융감독원 보안 가이드라인 준수
- 문서화 및 감사 준비
- 라이센스 신청

---

## 9. 다음 단계

1. ✅ 이 문서 검토 및 피드백
2. ⏳ 승인 후 개발 시작
3. ⏳ 프로토타입 배포 및 테스트
4. ⏳ 사용자 피드백 수집
5. ⏳ 고도화 및 프로덕션 준비

---

## 질문 및 확인 사항

1. **제품명 확정**: MoneyHub, MoneyLab, 대출청진기 중 어떤 이름을 공식 제품명으로 사용하시겠습니까?

2. **규제 범위**: "금융상품판매대리·중개업"만 신청 예정인가요, 아니면 "온라인대출모집법인"도 포함인가요?

3. **클라우드 선호도**: AWS Seoul, Naver Cloud, KT Cloud 중 선호하는 클라우드가 있으신가요?

4. **외부 API**: 향후 연동 예정인 외부 API가 있나요? (예: PASS/NICE 휴대폰 인증, Kakao 주소 검색, ChannelTalk 등)

5. **이 DB 구조와 워크플로우로 진행해도 괜찮으신가요?**
